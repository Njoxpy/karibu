const getUrl = (url) => {
  const category = "animal-feeding";
  const apiUrl = "http://localhost:5000/api/v1/";
  switch (category) {
    case "animal-feeding":
      catUrl = "animal-feeding";
      break;

    default:
      break;
  }
};
